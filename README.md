# Farm Cost Tracking System

Small Streamlit app to track and visualize farm costs using a CSV dataset.

## Files
- `app.py` — Streamlit application entrypoint
- `farm_data.csv` — sample dataset used by the app
- `requirements.txt` — Python dependencies

## Quick start

1. (Optional) Create and activate a virtual environment:

```bash
python3 -m venv .venv
source .venv/bin/activate
```

2. Install dependencies:

```bash
pip install -r requirements.txt
```

3. Run the app:

```bash
streamlit run app.py
```

## Notes
- The app expects `farm_data.csv` to be present in the project root.
- If you want a `.gitignore` or a longer README with screenshots, tell me and I will add it.

## Repository
This project is pushed to: https://github.com/NipulK/Farm-Cost-Calculation-App.git

---
Generated by GitHub Copilot assistant.
